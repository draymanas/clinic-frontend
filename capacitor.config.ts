import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.doctorplatform.app',
  appName: 'Doctor Platform',
  webDir: 'build'
};

export default config;
